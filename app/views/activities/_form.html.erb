<div class="container">
  <div class="row full-height">
    <div class="col-xs-12 col-md-4 col-md-offset-4 small-padding background-color height-full-percent">
      <div class="full-container overflow-scroll">
            <div class="filter-title">
              Add a new Activity
            </div>
              <%= simple_form_for @activity do |f| %>
               <div class="form-inputs">
                  <%= f.input :title, label: false, placeholder:"title"%>
                  <%= f.input :description, label: false, placeholder:"description" %>
                  <%= f.input :category_id, collection: Category.all, as: :select, label: false, include_blank: "-- Select category --" %>
                  <%= f.input :duration, label: false, placeholder:"duration in minutes" %>
                  <%= f.input :address, id:"activity_address", label: false, placeholder:"address" %>
                  <%= f.input :price, label: false, as: :currency, currency: "€" %>
                  <%= f.input :payable, label: "able to pay online?" %>
                  <%= f.input :picture %>
                  <%= f.input :picture_cache, as: :hidden %>
                </div>
      </div>
      <div class="col-xs-4 buttons-container">
        <button class="back-button passive-button half-width" onclick="goBack()"><i class="fas fa-caret-left"></i>Back</button>
        <div id="next-filter" class="next-button cta-button half-width"><%= f.button :submit, "Create!" ,class: "next-button cta-button"%></div>
      </div>
              <% end %>
    </div>
  </div>
</div>

<script>
function goBack() {
    window.history.back();
}

function goForward() {
    window.history.forward()
}
</script>
