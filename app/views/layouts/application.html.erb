<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Primetime</title>
    <%= yield :head %>
    <%= csrf_meta_tags %>
    <%= action_cable_meta_tag %>
    <link href="https://fonts.googleapis.com/css?family=Montserrat|Roboto" rel="stylesheet">
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= stylesheet_pack_tag 'application' %>
    <!-- <script src="https://raw.githubusercontent.com/HPNeo/gmaps/master/gmaps.js"></script> -->
    <%#= stylesheet_pack_tag 'application', media: 'all' %> <!-- Uncomment if you import CSS in app/javascript/packs/application.js -->
  </head>

  <body class="<%= controller_name %> <%= action_name %>">
    <%= render 'shared/slideout' %>
    <%= render 'shared/slideoutright' %>

    <div id="panel">
      <%= render 'shared/navbar' %>
      <%= render 'shared/flashes' %>
      <%= javascript_include_tag "https://maps.googleapis.com/maps/api/js?libraries=places&sensor=true&key=#{ENV['GOOGLE_API_BROWSER_KEY']}" %>
      <%= yield %>

      <%= javascript_include_tag 'application' %>
      <%= javascript_pack_tag 'application' %>

      <%= yield :after_js %>

      <%= yield :after_timer %>
      <%= yield :after_panel %>

    </div>
  </body>
</html>
