<%= simple_form_for [@booking, @review] do |f| %>
 <div class="form-inputs">
    <%= f.input :content %>

  </div>

  <div class="form-actions">
    <%= f.submit 'Rate it!', class: 'rate-button cta-button' %>
  </div>
<% end %>
